# General third-party dependencies (excluding PyTorch and Torchvision, 
# which are loaded separately via the official CPU-only index in our Dockerfile).
numpy==1.26.4
pandas==2.1.4
pyarrow==14.0.2
Pillow==10.3.0
matplotlib==3.9.1
scikit-learn==1.7.2

